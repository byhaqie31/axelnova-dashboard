{
  "name": "axelnova-mcp-connector",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker MCP server that lets Claude draft quotations in axelnova-dashboard via the scoped /v1/connector API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "rotate-token": "./rotate-token.sh",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "^0.17.3",
    "hono": "^4.12.0",
    "zod": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.107.0"
  }
}
